#ifndef EULER_H
#define EULER_H

#include <vector>
#include <cmath>
#include <cstdint>

std::vector<uint64_t> primeFactors(uint64_t n);
bool isPrime(int n);
bool isPalindrome(int n);
int *bubbleSort(int *arr);

#endif
